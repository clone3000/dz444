version: '3.8'

services:
  todo-app:
    build: .
    container_name: todo_app
    ports:
      - "8000:80"
    volumes:
      - todo_data:/app/data
    restart: unless-stopped

volumes:
  todo_data: